# OS files
# Windows thumbnail cache
Thumbs.db
Desktop.ini

# Python compiled files
__pycache__/
*.py[cod]
*$py.class

# Python envs and packaging
.env
.env.*
venv/
venv*/
.venv/
env/
ENV/
.Python
pip-wheel-metadata/
pip-selfcheck.json

# Django & general project files
db.sqlite3
*.sqlite3
local_settings.py
# user uploaded files
# (media should not be committed; keep local uploads out of git)
/media/
!media/.gitkeep

# Tailwind / frontend build outputs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# Tailwind / build caches
.tailwindcss/
.tailwind-cache/
.tailwindcache/

# Built CSS / dist folders (common targets)
# ignore the dist folder contents by default but keep committed build CSS if desired
theme/static/css/dist/*
!theme/static/css/dist/styles.css
static/src/.tmp/
staticfiles/src/.tmp/

# Ignore the app's static files (images, icons, builds)
# large trees (static and collected staticfiles) are ignored to avoid thousands
# of assets being added unintentionally. If you need to keep a small set of
# vendor files, prefer explicit whitelists (see example below).
/static/
/staticfiles/

# Example: whitelist only specific vendor asset types (uncomment and adapt)
# !staticfiles/vendor/**/*.min.css
# !staticfiles/vendor/**/*.min.js
# !staticfiles/vendor/**/*.woff2
# !staticfiles/vendor/**/*.woff
# !staticfiles/vendor/**/*.svg

# Editor and IDE
.vscode/
*.code-workspace
.idea/
*.sublime-project
*.sublime-workspace

# Logs, coverage, caches
*.log
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# Byte-compiled / distribution
build/
dist/
*.egg-info/
.eggs/
*.egg

# OS X
.DS_Store

# Windows
Thumbs.db

# Keys and certificates
*.pem
*.key

# Misc
*.pot
*.mo
*.swp

# Ignore secrets files often used in CI/local
secrets.json
.env.local

# Ignore IDE workspace specific files
.vscode/*

# If you use Docker, uncomment these
# .docker/
# docker-compose.override.yml
.backup-*


